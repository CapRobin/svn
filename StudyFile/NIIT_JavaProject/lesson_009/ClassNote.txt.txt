1、如何使用JS
    1）内部脚本
    2）外部脚本Script内容体里面写代码
    注意：
        1）内部和外部结合使用时全局变量的函数不要同名；
           如果同名会被覆盖或报错
        2）访问dom(HTML的树形结构)，访问dom元素时要么等待元素被浏览器显示
2、JS变量

3、Js函数

4、运算符

5、JS语句
    1）if语句
    2）Switch语句
    3）for循环
    4）while循环

6、JS对象（基本数据类型）
    所有对象的顶级父类都是Object(object)
    1、数据类型
        1）字符串()
        2）数字(Number|Number)，有整数和小数
        3）布尔型 (Boolean) 只有true|false
        4）数组 (Array|array)
        5）对象
    备注：
        1、使用typeOf可以输出数据的类型
        2、使用instanceOf来判断变量时什么数据类型
7、JS事件

7、Html里面的标签